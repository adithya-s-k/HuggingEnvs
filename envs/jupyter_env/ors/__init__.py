"""Jupyter Agent — ORS (Open Reward Standard) implementation.

Server:
    python -m environments.jupyter_agent.ors.server

Client:
    from openreward import EnvironmentsAPI
    api = EnvironmentsAPI(base_url="http://localhost:8080", api_key="")
"""
