# Copyright 2026 The HuggingFace Team. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-data-agent-env"
version = "0.1.0"
description = "Data-analysis agent tasks: an agent reads real tables in a sandbox and files an answer"
# 3.12, not 3.10: the capture stack and `openenv.core.sandbox` both require it.
requires-python = ">=3.12"
dependencies = [
    # Brings the capture proxy and the harness contract. The sandbox backends are vendored into
    # sandbox/ rather than imported, because openenv-opencode-env is not published to PyPI.
    "openenv>=0.3.1",
    "datasets>=2.19",
    "huggingface_hub>=1.12",
    "fastapi>=0.115",
    "fastmcp>=2.0",
    "httpx>=0.27",
    "pydantic>=2.0",
    "uvicorn[standard]>=0.24",
]

# Sandbox backends are OPTIONAL and independent: a deployment installs only the one it will use,
# and `sandbox/__init__.py` imports each lazily, so a missing SDK for one never breaks the other.
# The backend code itself is VENDORED into sandbox/ (see its docstring) -- these are just its SDKs.
[project.optional-dependencies]
e2b = ["e2b>=1.0.0"]
# `huggingface_hub.Sandbox` ships in 1.22; there is no `[sandbox]` extra, and asking for one makes
# uv warn and then silently resolve without it.
hf = ["huggingface_hub>=1.22"]
daytona = ["daytona>=0.203.0"]
dev = ["pytest>=9.0.3", "pytest-cov>=4.0.0"]

[project.scripts]
server = "data_agent_env.server.app:main"

[tool.setuptools]
include-package-data = true
packages = ["data_agent_env", "data_agent_env.server", "data_agent_env.sandbox"]
package-dir = { "data_agent_env" = ".", "data_agent_env.server" = "server", "data_agent_env.sandbox" = "sandbox" }
