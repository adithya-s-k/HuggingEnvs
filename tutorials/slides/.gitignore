# local reference material (cloned repos, articles) — not committed
reference/
node_modules/
dist/
.DS_Store
*.log
*.tsbuildinfo

# large uncropped screenshot sources (only the *-crop versions are used)
src/assets/openai-ss2.png
src/assets/openai-ss3.png
